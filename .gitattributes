# The shader sources are HTML/GLSL payloads imported as raw strings by the effect components,
# not code anyone edits here. Keep them out of the repository's language stats.
landing/src/shaders/*/sources/*.html linguist-vendored
